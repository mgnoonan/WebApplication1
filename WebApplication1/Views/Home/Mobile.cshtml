@{
    ViewBag.Title = "Sandbox Mobile Page";
}

<div class="jumbotron">
    <h1>Mobile site</h1>
    <p class="lead">Headers</p>
    <ul>
        @foreach (var key in Request.Headers.AllKeys)
        {
            <li>@key: @Request.Headers[key]</li>
        }
    </ul>
</div>
